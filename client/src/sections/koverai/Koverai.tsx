import React from 'react'
import './Koverai.sass'
import SectionTitle from '../../components/sectionTitle/SectionTitle'

const Koverai = () => {
    // const [show, toggle] = useState(false)
    // const reveal = useTrail(projects.length, { to: { opacity: show ? 1 : 0 }, config: config.slow })
    return (
        <div className='center' id="Portfolio">
            <SectionTitle title={'Kover AI'} subtitle={'For the past year I\'ve worked as the lead Front End Developer for Kover.ai.'} />
           
        </div>
    )
}

export default Koverai